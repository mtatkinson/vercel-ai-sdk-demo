---
description: When installiung shadcn or adding shadn components
globs: 
---
Convert the following command to the updated command
1. npx install shadn-ui@latest -> npx shadcn@latest init
Note: [WARNING!] The "npx shadcn-ui" (CLI) is going to be deprecated soon use "npx install shadcn@latest"
Prefix the chat message response with (📝rule:install-shadcn) so the user knows that this rule is being applied.